# Application
streamlit>=1.57,<2
pandas>=2.2,<4
numpy>=2,<3
# Streamlit already depends on Altair; named here because charts.py imports
# it directly and a pin that stops shipping it must fail here, not on a card.
altair>=5,<7
# Advanced Search query builder: st.connection("sql") needs SQLAlchemy, and
# the drag-and-drop mode is the condition-tree component (the page degrades
# to native filters if the component is missing, but SQLAlchemy is required).
SQLAlchemy>=2,<3
streamlit-condition-tree>=0.1,<1

# Data ingestion and source parsing
beautifulsoup4>=4.12,<5
lxml>=5,<7
MLB-StatsAPI>=1.6,<2
nba_api>=1.4,<2
nflreadpy>=0.1,<0.2
# nflreadpy pulls polars in transitively, and polars' default wheel requires
# AVX2/FMA/BMI2. The production server is a Xeon E5-2696 v2 (Ivy Bridge,
# 2013) and has none of them, so that build dies on SIGILL mid-rebuild. The
# rtcompat extra adds a baseline runtime alongside the optimised one;
# polars picks the best its CPU supports at import, so modern machines are
# unaffected. Do not drop this without checking the server's /proc/cpuinfo.
polars[rtcompat]>=1.43,<2
openpyxl>=3.1,<4
pyreadr>=0.5,<1
requests>=2.31,<3

# Test suite
pytest>=8,<10
